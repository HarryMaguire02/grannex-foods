'use client';

interface ContactPopupProps {
  isOpen: boolean;
  onClose: () => void;
}

// Stub — full implementation coming in next iteration
export default function ContactPopup(_props: ContactPopupProps) {
  return null;
}
